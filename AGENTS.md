## Cursor Cloud specific instructions

### Repository state

This repository is currently **documentation-only**. It contains planning/design
documents (`README.md`, `REQUERIMIENTOS`, `SYSTEMBRIEF`) for an inventory
management system called **InventarioPYME**. No source code, build system, or
runnable application exists yet.

### Proposed tech stack (from README.md)

| Layer        | Technology           |
|--------------|----------------------|
| Frontend     | React + TypeScript   |
| Backend      | Node.js + Express    |
| Database     | PostgreSQL           |
| Email alerts | SendGrid             |
| Excel export | SheetJS              |
| PDF export   | jsPDF                |

### What can be run

- **Lint / Tests / Build / Dev server**: None — no source code exists.
- There is no `package.json`, `Makefile`, `Dockerfile`, or any build configuration.

### Environment

- Node.js v22 and npm are available in the VM.
- Once source code is added, the update script and this section should be revised.
